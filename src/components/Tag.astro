---
const { tag } = Astro.props;
---

<a
    href={`/tags/${tag.id}`}
    class="bg-red-100 text-red-800 font-medium mx-1 px-2.5 py-0.5 rounded dark:bg-gray-700 dark:text-red-400 border border-red-400"
    >{`${tag.name}`}</a
>
